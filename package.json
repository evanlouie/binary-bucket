{
  "name": "binary-bucket",
  "version": "1.0.0",
  "description": "General Node.js API for popular cloud storage solutions.",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf coverage build tmp",
    "build": "tsc -p tsconfig.release.json",
    "watch": "tsc -w -p tsconfig.release.json",
    "lint": "tslint -t stylish --type-check --project 'tsconfig.json'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/evanlouie/binary-bucket.git"
  },
  "keywords": ["node", "azure", "s3", "blob", "storage", "typescript"],
  "author": "Evan Louie",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/evanlouie/binary-bucket/issues"
  },
  "homepage": "https://github.com/evanlouie/binary-bucket#readme",
  "devDependencies": {
    "@types/node": "^8.0.10",
    "rimraf": "^2.6.1",
    "tslint": "^5.5.0",
    "tslint-microsoft-contrib": "^5.0.1",
    "typescript": "^2.4.1"
  }
}
